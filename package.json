{
  "name": "e-commerce-shop",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=!@repo/api --filter=!@repo/db",
    "build": "turbo build --filter=!@repo/api --filter=!@repo/db",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "@prisma/client",
      "@prisma/engines",
      "prisma"
    ]
  }
}
